import { roboto_mono } from '../fonts'

export default function Projects() {
  return (
    <main className='max-w-screen-lg w-full'>
      <section>
        <h2 className={`${roboto_mono.className} text-lg`}>What do I work on?</h2>
      </section>
    </main>
  )
}
